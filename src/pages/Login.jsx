import FormLogin from '../components/FormLogin/FormLogin';

const Login = () => {

        return (
            <main>
                <FormLogin />
            </main>
        )
    };

export default Login;